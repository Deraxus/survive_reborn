using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Класс для гибридных врагов.
// Например, враг, который стреляет и бьет вблизи, а также имеет специальные атаки
public class HybridAttackState : AttackState
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
